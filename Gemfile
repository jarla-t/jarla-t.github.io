source "https://rubygems.org"

gem "github-pages", "~> 228", group: :jekyll_plugins
gem "csv"
gem "webrick"

# For Windows, improves file-change detection
gem 'wdm', '>= 0.1.0', require: false if Gem.win_platform?

# Needed for Faraday retry middleware (optional if you use Octokit or remote APIs)
gem 'faraday-retry', '~> 1.0'

# Needed for Ruby >= 3.5 to avoid fiddle warnings (optional for now)
gem 'fiddle', '~> 1.0'
